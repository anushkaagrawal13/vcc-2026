"""Session 3: LightGBM after ridge, using identical features and held-out split.

Add a pinned dependency only when this experiment is implemented. CPU threads,
seed, feature set and hyperparameters belong in its own config/*.yaml.
"""
